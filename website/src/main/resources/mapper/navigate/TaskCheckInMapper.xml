<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chen.mapper.TaskCheckInMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, task_id, check_date, check_status, check_content, check_note, actual_start_time, actual_end_time, create_time, update_time, create_by, update_by, del_flag
    </sql>

    <!-- 分页查询任务打卡记录 -->
    <select id="selectTaskCheckInPage" resultType="com.chen.domain.taskcheckin.TaskCheckInPO">
        select
        <include refid="Base_Column_List"/>
        from cyz_task_check_in
        <where>
            del_flag = '0'
            <if test="taskPlanId != null">
                and task_id = #{taskPlanId}
            </if>
            <if test="startTime != null">
                and check_date &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                and check_date &lt;= #{endTime}
            </if>
        </where>
        order by check_date desc
    </select>

    <!-- 查询指定日期范围内的任务打卡记录 -->
    <select id="selectTaskCheckInList" resultType="com.chen.domain.taskcheckin.TaskCheckInPO">
        select
        <include refid="Base_Column_List"/>
        from cyz_task_check_in
        <where>
            del_flag = '0'
            <if test="startTime != null">
                and check_date &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                and check_date &lt;= #{endTime}
            </if>
        </where>
        order by check_date desc
    </select>

    <!-- 查询指定日期的任务打卡记录 -->
    <select id="selectTaskCheckInListByDate" resultType="com.chen.domain.taskcheckin.TaskCheckInPO">
        select
        <include refid="Base_Column_List"/>
        from cyz_task_check_in
        <where>
            del_flag = '0'
            and check_date = #{date}
        </where>
        order by check_date desc
    </select>

    <!-- 查询指定任务计划的打卡记录 -->
    <select id="selectTaskCheckInListByTaskPlan" resultType="com.chen.domain.taskcheckin.TaskCheckInPO">
        select
        <include refid="Base_Column_List"/>
        from cyz_task_check_in
        <where>
            del_flag = '0'
            and task_id = #{taskPlanId}
        </where>
        order by check_date desc
    </select>

    <!-- 统计指定日期范围内的打卡完成情况 -->
    <select id="selectTaskCheckInStatistics" resultType="com.chen.domain.taskcheckin.TaskCheckInPO">
        select
            check_date,
            count(*) as total_count,
            sum(case when check_status = '1' then 1 else 0 end) as completed_count
        from cyz_task_check_in
        <where>
            del_flag = '0'
            <if test="startTime != null">
                and check_date &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                and check_date &lt;= #{endTime}
            </if>
        </where>
        group by check_date
        order by check_date desc
    </select>

</mapper> 