<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chen.mapper.TaskPlanMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, task_type, task_name, task_content, start_time, end_time, status, create_time, update_time, create_by, update_by, del_flag
    </sql>

    <!-- 分页查询任务计划列表 -->
    <select id="selectTaskPlanPage" resultType="com.chen.domain.taskplan.TaskPlanPO">
        select
        <include refid="Base_Column_List"/>
        from cyz_task_plan
        <where>
            del_flag = '0'
            <if test="taskType != null and taskType != ''">
                and task_type = #{taskType}
            </if>
            <if test="startTime != null">
                and start_time &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                and end_time &lt;= #{endTime}
            </if>
        </where>
        order by create_time desc
    </select>

    <!-- 查询指定日期范围内的任务计划列表 -->
    <select id="selectTaskPlanList" resultType="com.chen.domain.taskplan.TaskPlanPO">
        select
        <include refid="Base_Column_List"/>
        from cyz_task_plan
        <where>
            del_flag = '0'
            <if test="startTime != null">
                and start_time &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                and end_time &lt;= #{endTime}
            </if>
        </where>
        order by create_time desc
    </select>

    <!-- 查询指定日期的任务计划列表 -->
    <select id="selectTaskPlanListByDate" resultType="com.chen.domain.taskplan.TaskPlanPO">
        select
        <include refid="Base_Column_List"/>
        from cyz_task_plan
        <where>
            del_flag = '0'
            and start_time &lt;= #{date}
            and end_time &gt;= #{date}
        </where>
        order by create_time desc
    </select>

</mapper> 